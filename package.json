{
  "name": "future-life-diagnostics",
  "version": "1.0.0",
  "description": "Company profile website for Future-Life Diagnostics Laboratory & Medical Technologies",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "sync:docs": "node scripts/sync-docs.mjs",
    "build:deploy": "npm run build && npm run sync:docs",
    "dev": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
